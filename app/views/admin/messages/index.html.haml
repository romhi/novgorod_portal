.top-right-actions
  = link_to "Написать", new_admin_message_path, class: "btn btn-primary"
%h1 Переписка

%table.table.table-bordered.table-condensed.table-hover
  %tr
    %th.td-18
      \#
    %th
      Пользователь
    %th
      Сообщения

  - @messages.each_with_index do |message, index|

    %tr
      %td.td-18
        = index + 1
      %td
        = message.user.full_info
      %td
        = link_to "Новые: #{message.new.to_i} / Всего: #{message.qty}", admin_message_path(message.id), class: "btn-sm btn-primary"

